import { } from "../types";

const initialState = {
  partners : {
    kwork: '?ref=1375553',
    flru: '?ref=169430'
  },
}

export default function partners(state = initialState){

  return  state
}